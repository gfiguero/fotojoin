{% extends 'FotoJoinControlPanelBundle::base.html.twig' %}

{% block title %}{{ 'city.index'|trans }}{% endblock %}

{% block body %}
    <div id="page-wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">{% include 'FotoJoinControlPanelBundle::icon.html.twig' %}{{ 'city.list'|trans }}</h1>
                {% include 'FotoJoinControlPanelBundle::flashbag.html.twig' %}
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <div class="btn-toolbar">
                            <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#modalNew">{{ icon('plus') }}&nbsp;{{ 'city.new'|trans }}</button>
                            <div class="modal fade" id="modalNew" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                                <div class="modal-dialog modal-sm" role="document">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                                            <h4 class="modal-title" id="myModalLabel">{{ 'city.new'|trans }}</h4>
                                        </div>
                                        <div class="modal-body">
                                            {{ form_start(newForm, { 'style': 'horizontal' }) }}
                                            {{ form_widget(newForm) }}
                                                <div class="form-group"><div class="col-lg-offset-4 col-lg-8">
                                                    <button type="submit" class="btn btn-primary">{{ icon('plus') }}&nbsp;{{ 'city.register'|trans }}</button>
                                                </div></div>
                                            {{ form_end(newForm) }}
                                        </div>
                                    </div>
                                </div>
                            </div>

                            {#}<a class="btn btn-primary" href="{{ path('city_new') }}">{{ icon('file') }}&nbsp;{{ 'city.new'|trans }}</a>{#}
                        </div>
                        <div class="clearfix"></div>
                    </div>
                    <div class="table-responsive">
                        <table class="records_list table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>{{ knp_pagination_sortable(cities, 'city.name'|trans, 'name') }}{% if sort == 'name' %}{% if direction == 'desc' %}{{ icon('caret-down') }}{% else %}{{ icon('caret-up') }}{% endif %}{% endif %}</th>
                                    <th>{{ 'city.actions'|trans }}</th>
                                </tr>
                            </thead>
                            <tbody>
                            {% for key, city in cities %}
                                <tr>
                                {#}<tr class='clickableRow' href="{{ path('city_show', { 'id': city.id }) }}">{#}
                                    <td>{% if city.name %}{{ city.name }}{% endif %}</td>
                                    <td>
                                        <button type="button" class="btn btn-primary btn-sm" data-toggle="modal" data-target="#modalEdit{{ city.id }}">{{ icon('edit fa-fw') }}{{ 'city.edit'|trans }}</button>
                                        <div class="modal fade" id="modalEdit{{ city.id }}" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                                            <div class="modal-dialog modal-sm" role="document">
                                                <div class="modal-content">
                                                    <div class="modal-header">
                                                        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                                                        <h4 class="modal-title" id="myModalLabel">{{ 'city.edit'|trans }}</h4>
                                                    </div>
                                                    <div class="modal-body">
                                                        {% set editForm = attribute(editForms, key) %}
                                                        {{ form_start(editForm, { 'style': 'horizontal' }) }}
                                                        {{ form_widget(editForm) }}
                                                            <div class="form-group"><div class="col-lg-offset-4 col-lg-8">
                                                                <button type="submit" class="btn btn-primary">{{ icon('check fa-fw') }}&nbsp;{{ 'city.edit'|trans }}</button>
                                                            </div></div>
                                                        {{ form_end(editForm) }}
                                                    </div>
                                                </div>
                                            </div>
                                        </div>

                                        <button type="button" class="btn btn-danger btn-sm" data-toggle="modal" data-target="#modalDelete{{ city.id }}">{{ icon('times fa-fw') }}{{ 'city.delete'|trans }}</button>
                                        <div class="modal fade" id="modalDelete{{ city.id }}" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                                            <div class="modal-dialog modal-sm" role="document">
                                                <div class="modal-content">
                                                    <div class="modal-header">
                                                        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                                                        <h4 class="modal-title" id="myModalLabel">{{ 'city.delete'|trans }}</h4>
                                                    </div>
                                                    <div class="modal-body">
                                                        <div class="col-xs-2 text-center">{{ icon('exclamation-triangle fa-5x') }}</div>
                                                        <div class="col-xs-10"><p>{{ 'city.deletemessage'|trans }}</p></div>
                                                        <div class="clearfix"></div>
                                                    </div>
                                                    <div class="modal-footer">
                                                        {% set deleteForm = attribute(deleteForms, key) %}
                                                        {{ form_start(deleteForm) }}
                                                            {{ form_widget(deleteForm) }}
                                                            <button type="submit" class="btn btn-danger pull-right">{{ icon('times fa-fw') }}{{ 'city.delete'|trans }}</button>
                                                        {{ form_end(deleteForm) }}
                                                        <div class="clearfix"></div>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>

                                    </td>
                                </tr>



                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                    <div class="panel-footer">
                        <div class="text-center">
                            {{ knp_pagination_render(cities) }}
                        </div>
                        <div class="clearfix"></div>
                    </div>
                </div>
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
    </div>
    <!-- /#page-wrapper -->

{% endblock %}
