{% extends 'FotoJoinAdminBundle:City:layout.html.twig' %}

{% trans_default_domain 'FotoJoinAdminBundle' %}

{% block title %}{{ 'city.delete.title'|trans }}{% endblock %}

{% block city %}

    <div class="row" id="page-delete">
        <div class="col-lg-6">
            <div class="panel panel-danger">
                <div class="panel-heading">
                    <h2 class="panel-title">{{ 'city.delete.title'|trans }}</h2>
                </div>
                <div class="panel-body">
                    <dl class="dl-horizontal">
                        <dt>{{ 'city.form.id'|trans }}</dt><dd>{% if city.id %}{{ city.id }}{% endif %}</dd>
                        <dt>{{ 'city.form.name'|trans }}</dt><dd>{% if city.name %}{{ city.name }}{% endif %}</dd>
                    </dl>
                </div>
            </div>
        </div>
        {{ form_start(deleteForm) }}
        {{ form_widget(deleteForm) }}
        <div class="col-md-6">
            <div class="btn-toolbar">
                <div class="btn-group"><button type="submit" class="btn btn-danger">{{ icon('times fa-fw') }}&nbsp;{{ 'city.delete.submit'|trans }}</button></div>
                <div class="btn-group"><a type="button" class="btn btn-default" href="{{ path('admin_city_show', {'id' : city.id}) }}">{{ icon('times fa-fw') }}&nbsp;{{ 'city.delete.cancel'|trans }}</a></div>
            </div>
        </div>
        {{ form_end(deleteForm) }}
    </div>
    <!-- /#page-delete -->

{% endblock %}
