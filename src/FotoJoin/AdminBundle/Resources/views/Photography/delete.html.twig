{% extends 'FotoJoinAdminBundle:Photography:layout.html.twig' %}

{% trans_default_domain 'FotoJoinAdminBundle' %}

{% block title %}{{ 'photography.delete.title'|trans }}{% endblock %}

{% block photography %}

    <div class="row" id="page-delete">
        <div class="col-lg-6">
            <div class="panel panel-danger">
                <div class="panel-heading">
                    <h2 class="panel-title">{{ 'photography.delete.title'|trans }}</h2>
                </div>
                <div class="panel-body">
                    <dl class="dl-horizontal">
                        <dt>{{ 'photography.form.id'|trans }}</dt><dd>{% if photography.id %}{{ photography.id }}{% endif %}</dd>
                        <dt>{{ 'photography.form.filename'|trans }}</dt><dd>{% if photography.filename %}{{ photography.filename }}{% endif %}</dd>
                    </dl>
                </div>
            </div>
        </div>
        {{ form_start(deleteForm) }}
        {{ form_widget(deleteForm) }}
        <div class="col-md-6">
            <div class="btn-toolbar">
                <div class="btn-group"><button type="submit" class="btn btn-danger">{{ icon('times fa-fw') }}&nbsp;{{ 'photography.delete.submit'|trans }}</button></div>
                <div class="btn-group"><a type="button" class="btn btn-default" href="{{ path('admin_photography_show', {'id' : photography.id}) }}">{{ icon('times fa-fw') }}&nbsp;{{ 'photography.delete.cancel'|trans }}</a></div>
            </div>
        </div>
        {{ form_end(deleteForm) }}
    </div>
    <!-- /#page-delete -->

{% endblock %}
