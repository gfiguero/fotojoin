{% block extends %}
{{ "{% extends ':Admin:base.html.twig' %}" }}
{% endblock extends %}

{% block body %}
{{ "{% block title %}" }}{{ "{{ '" ~ entity_singularized ~ ".edit'|trans }}" }}{{ "{% endblock %}" }}

{{ "{% block body %}" }}
    <div id="page-wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">{{ "{% include ':Admin:icon.html.twig' %}" }}{{ "{{ '" ~ entity_singularized|lower ~ ".edit'|trans }}" }}</h1>
                {{ "{% include ':Admin:flashbag.html.twig' %}" }}
            </div>
            <!-- /.col-lg-12 -->
        </div>

        <!-- /.row -->
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">

                    {%- set hide_edit, hide_delete = true, false -%}
                    {{- include('crud/views/others/record_actions.html.twig.twig') }}
                    <div class="panel-body">
                        {{ "{{ form_start(edit_form, { 'style': 'horizontal' }) }}" }}
                            {{ '{{ form_widget(edit_form) }}' }}
                                <div class="form-group"><div class="col-lg-offset-2 col-lg-10">
                                    <button type="submit" class="btn btn-primary">{{ "{{ icon('save') }}&nbsp;{{ '" ~ entity_singularized|lower ~ ".save'|trans }}" }}</button>
                                </div></div>
                        {{ '{{ form_end(edit_form) }}' }}

                    </div>
                </div>
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
    </div>
    <!-- /#page-wrapper -->

{{ "{% endblock %}" }}
{% endblock body %}

