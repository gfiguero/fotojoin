{% extends "FotoJoinFrontPageBundle::base.html.twig" %}

{% trans_default_domain 'FotoJoinUserBundle' %}

{% block title %}{{ 'registration.title'|trans }}{% endblock %}

{% block body %}

<div class="section fill" id="register">
    <div class="container">
        <div class="row">
            <div class="col-lg-8 col-lg-offset-2">
                <div class="transparent-square">
                    <h1 class="page-header">{{ 'registration.title'|trans }}</h1>
                    <div class="panel panel-default">
                        <div class="panel-body text-center">
                            <p>{{ 'registration.check_email'|trans({'%email%': user.email}) }}</p>
                            {#<a href="{{ path('fos_user_registration_register') }}" type="button" class="btn btn-primary">{{ icon('save') }}&nbsp;{{ 'registration.back'|trans }}</a>#}
                            {#<a class="btn btn-primary" href="{{ path('fos_user_security_login') }}" role="button">{{ icon('sign-in') }}&nbsp;{{ 'link.login'|trans }}</a>#}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div id="push"></div>
</div>

{% include 'FotoJoinFrontPageBundle::footer.html.twig' %}

{% endblock body %}
