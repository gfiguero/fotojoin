{% extends ':ControlPanel:base.html.twig' %}

{% block title %}{{ 'capture.show'|trans }}{% endblock %}

{% block body %}
    <div id="page-wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">{% include ':ControlPanel:icon.html.twig' %}{{ 'capture.show'|trans }}</h1>
                {% include ':ControlPanel:flashbag.html.twig' %}
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                                        
                    <div class="panel-heading">
                        <div class="btn-toolbar">
                            <a type="button" class="btn btn-default" href="{{ path('capture_index') }}">{{ icon('list') }}&nbsp;{{ 'capture.list'|trans }}</a>
                            <a type="button" class="btn btn-warning" href="{{ path('capture_edit', { 'id': capture.id }) }}">{{ icon('pencil') }}&nbsp;{{ 'capture.edit'|trans }}</a>
                            <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#modal-confirm">{{ icon('trash-o') }}&nbsp;{{ 'capture.delete'|trans }}</button>
                            <div class="modal fade" id="modal-confirm" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                                <div class="modal-dialog modal-md" role="document">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                                            <h4 class="modal-title" id="myModalLabel">{{ 'Are you sure?'|trans }}</h4>
                                        </div>
                                        <div class="modal-body">
                                            <p>{{ 'Deleted records can not be recovered.'|trans }}</p>
                                        </div>
                                        <div class="modal-footer">
                                            <div class="pull-right" style="height:34px;">
                                                {{ form_start(delete_form) }}
                                                    <button type="submit" class="btn btn-danger">{{ icon('trash-o') }}&nbsp;{{ 'capture.delete'|trans }}</button>
                                                {{ form_end(delete_form) }}</div>
                                            <div class="clearfix"></div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div><div class="panel-body">
                        <dl class="dl-horizontal dl-horizontal-sm dl-horizontal-md dl-horizontal-lg">
                                <dt>{{ 'capture.brand'|trans }}</dt>
                                    <dd>{% if capture.brand %}{{ capture.brand }}{% endif %}</dd>
                                <dt>{{ 'capture.model'|trans }}</dt>
                                    <dd>{% if capture.model %}{{ capture.model }}{% endif %}</dd>
                                <dt>{{ 'capture.lens'|trans }}</dt>
                                    <dd>{% if capture.lens %}{{ capture.lens }}{% endif %}</dd>
                                <dt>{{ 'capture.sensibility'|trans }}</dt>
                                    <dd>{% if capture.sensibility %}{{ capture.sensibility }}{% endif %}</dd>
                                <dt>{{ 'capture.aperture'|trans }}</dt>
                                    <dd>{% if capture.aperture %}{{ capture.aperture }}{% endif %}</dd>
                                <dt>{{ 'capture.shutter'|trans }}</dt>
                                    <dd>{% if capture.shutter %}{{ capture.shutter }}{% endif %}</dd>
                                <dt>{{ 'capture.createdAt'|trans }}</dt>
                                    <dd>{% if capture.createdAt %}{{ capture.createdAt|localizeddate('full', 'full', 'es_ES', 'America/Santiago', "d 'de' LLLL 'de' Y - HH:mm") }}{% endif %}</dd>
                                <dt>{{ 'capture.updatedAt'|trans }}</dt>
                                    <dd>{% if capture.updatedAt %}{{ capture.updatedAt|localizeddate('full', 'full', 'es_ES', 'America/Santiago', "d 'de' LLLL 'de' Y - HH:mm") }}{% endif %}</dd>
                        </dl>
                    </div>
                </div>
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
    </div>
    <!-- /#page-wrapper -->
{% endblock %}

